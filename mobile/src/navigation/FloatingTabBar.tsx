import type { BottomTabBarProps } from '@react-navigation/bottom-tabs';
import { View } from 'react-native';

export const FloatingTabBar = (_props: BottomTabBarProps): JSX.Element => {
  return <View />;
};
